{% trans_default_domain 'JLasoTranslationsApiBundle' %}

{% extends '::JLaso/TranslationsApiBundle/base.html.twig' %}


{% block stylesheets %}
    {{ parent() }}
    <style type="text/css">
        .center { text-align: center; }
    </style>
{% endblock stylesheets %}


{% block subcontent %}


<div class="row-fluid">
    <div class="span12">
        <div>
            <legend class="span4">{{ 'admin.catalog.title'|trans }}</legend>
            {{ knp_pagination_render(pagination)|raw }}
        </div>

        <table class="table table-striped table-bordered table-condensed">
            <thead>
            <tr>
                <th>{{ 'admin.title'|trans }}</th>
                <th>{{ 'admin.actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>


            {% for entity in pagination %}
                <tr class="{% if loop.index is odd %}odd{% else %}even{% endif %}">
                    <td>{{ entity }}</td>
                    <td>
                        <a class="btn btn-primary" href="{{ path('jlaso_translationsapi_admin_catalog_list', { 'catalog': entity }) }}">
                            <i class="glyphicon glyphicon-eye-open" title="{{ 'admin.view'|trans }}"></i>
                        </a>
                    </td>
                </tr>

            {% endfor %}
            </tbody>
        </table>

        <div class="jlaso_blog_navigation">
            {{ knp_pagination_render(pagination)|raw }}
        </div>

        {% endblock subcontent %}


